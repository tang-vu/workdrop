WORKDROP_MODE=local
WORKDROP_DATA_DIR=./data
WORKDROP_HOST=127.0.0.1
WORKDROP_PORT=8000
# No private keys or paid API keys are needed for local mode.
# Optional read-only Solami observation; use a server-side, project-scoped key.
# SOLAMI_RPC_URL=https://rpc.solami.dev/sol?api_key=YOUR_KEY
# WORKDROP_LIVE_ADDRESSES=COMMA_SEPARATED_MINT_POOL_AND_PROGRAM_ADDRESSES
